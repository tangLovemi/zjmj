//* 停止音效 *
function stopEffect(id) {
    //if(cc.sys.OS_WINDOWS != cc.sys.os) {
    //    cc.audioEngine.stopEffect(id);
    //}
}
//* 播放音效 *
function playEffect(sd, sex) {
    //Log("playEffect sd:" + sd);
    //Log("playEffect jsclient.data.sData.tData:" + JSON.stringify(jsclient.data.sData.tData));
    //if(!sex && jsclient.data && jsclient.data.sData && jsclient.data.sData.tData && jsclient.data.sData.tData.gameid){
    //    if(GAME_TYPE_NAME.guandan == jsclient.data.sData.tData.gameid){
    //        return cc.audioEngine.playEffect("res/sound/gd/"+sd+".mp3",false);
    //    }else if(GAME_TYPE_NAME.doudizhu == jsclient.data.sData.tData.gameid){
    //        Log("app.js: playEffect() ddz");
    //        return cc.audioEngine.playEffect("res/sound/ddz/"+sd+".mp3",false);
    //    }
    //}
    //Log("app.js: playEffect()");
    //if(cc.sys.OS_WINDOWS != cc.sys.os){
    //    if("sex/" == sd.substring(0, 4)){
    //        sd = sd.substring(4);
    //        if(sex && sex == 1){
    //            sd = "sex/male/" + sd;
    //        }else{
    //            sd = "sex/female/" + sd;
    //        }
    //    }
    //    return cc.audioEngine.playEffect("res/sound/"+sd+".mp3",false); // zhibo command out for win32
    //}
}
//* 播放音乐 *
function playMusic(sd) {
    //if(cc.sys.OS_WINDOWS != cc.sys.os) {
    //    cc.audioEngine.stopMusic();
    //    cc.audioEngine.playMusic("res/sound/" + sd + ".mp3", true);  // zhibo command out for win32
    //}
}